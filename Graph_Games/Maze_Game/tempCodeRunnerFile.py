# Check if the mouse clicks the back button
                # elif event.type == pygame.MOUSEBUTTONDOWN:
                #     if self.back_button_rect.collidepoint(event.pos):
                #         print("Back button clicked. Exiting game...")
                #         pygame.quit()
                #         sys.exit()